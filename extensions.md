# Extensions (moved)

This page has been split up. The mechanism — extension ids, `HAS_PACKETS` and
`PACKETLESS`, the extension packet layout, the `ExtInfo` packet and the
negotiation flow — is now described in
[extensions/extension.html](extensions/extension.html), and the registry of
every known extension is listed in the
[0.75 documentation](protocol075.html#extensions).

Each extension is specified in its own file:

## Player Properties

Moved to [extensions/player-properties.html](extensions/player-properties.html).

## Player Limit

Moved to [extensions/player-limit.html](extensions/player-limit.html).

## Message Types

Moved to [extensions/message-types.html](extensions/message-types.html).

## Kick Reason

Moved to [extensions/kick-reason.html](extensions/kick-reason.html).

## UTF-8 Chat

Moved to [extensions/utf-8-chat.html](extensions/utf-8-chat.html).
